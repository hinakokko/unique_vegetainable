
      <% if user_signed_in? %>
  <div class="search_form">
    <%= form_with url: search_path, local: true, method: :get do |f| %>
      <tr>
        <td><%= f.text_field :word %></td>
        <td><%= f.select :range, options_for_select([['検索 ( 農家・購入先・産地・野菜 )']]) %></td>
        <td><%= f.select :search, options_for_select([["完全一致","perfect_match"], ["前方一致","forward_match"],  ["後方一致","backward_match"], ["部分一致","partial_match"]]) %></td>
        <td><%= f.submit "検索", class: "btn btn-warning " %></td>
      </tr>
    <% end %>
  </div>
<% end %>

